<header class="container-fluid border-bottom shadow-sm">
    <div class="container d-flex justify-content-between align-items-center" style=" min-height: 60px;">
        <div class="logo">
            <h4 class="fw-bold mb-0 c-green">Marker <i class="fa-solid fa-pen"></i></h4>
        </div>

        <div class="d-flex">
         <% if user_signed_in? %>
            <% if current_user.admin %>
                <%= link_to "Dashboard", admin_dashboard_index_path, class: "btn btn-sucess"%>
            <% end %>
            <%= button_to "log out", destroy_user_session_path ,class: "btn btn-success", method: :delete %>
        <%end%>
        </div>
    </div>


   
</header>